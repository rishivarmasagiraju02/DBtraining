Training Repository
Training push
Create GitHub Account - company email

SSH key - Laptop and GitHub (Authorization and Trust)

Creating Repo (Might already exists)

git clone <url> (Copies repo into your local machine)

git status (Check status for git repo locally)

Made some changes -files etc

git add <file>

git commit -m "<message>"

git push


git branch <branch name>

git checkout <branch name>

git push -u origin <branch name>

git status

git add 

git commit 

git push

PR is merged to main branch

git checkout main

git pull
hello this is the new changes
